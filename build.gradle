buildscript {
    repositories {
        mavenCentral()
        maven { url "https://plugins.gradle.org/m2/" }
    }
    dependencies {
        classpath "com.morpheusdata:morpheus-plugin-gradle:${morphPluginGradleVersion}"
    }
}

plugins {
    id 'groovy'
    id 'com.github.johnrengelman.shadow' version '7.1.2'
}

apply plugin: 'com.morpheusdata.morpheus-plugin-gradle'

group = 'com.morpheusdata'
version = '0.9.6'
sourceCompatibility = '11'
targetCompatibility = '11'

// Source has em-dashes/middle-dots in UI strings; without this a Windows build reads UTF-8 source as cp1252 and the page shows '?'/mojibake.
tasks.withType(JavaCompile) { options.encoding = 'UTF-8' }
tasks.withType(GroovyCompile) { options.encoding = 'UTF-8'; groovyOptions.encoding = 'UTF-8' }

repositories {
    mavenCentral()
    maven { url 'https://plugins.gradle.org/m2/' }
}

dependencies {
    compileOnly "com.morpheusdata:morpheus-plugin-api:${morphPluginApiVersion}"
    compileOnly "org.codehaus.groovy:groovy-all:${groovyVersion}"
    compileOnly 'io.reactivex.rxjava2:rxjava:2.2.21'
    compileOnly "org.slf4j:slf4j-api:${slf4jVersion}"

}

jar {
    manifest {
        attributes(
            'Plugin-Class'                  : 'com.morpheusdata.hvmupdater.HvmUpdaterPlugin',
            'Plugin-Version'                : version,
            'Morpheus-Name'                 : 'HVM Updater',
            'Morpheus-Organization'         : 'Travis DeLuca',
            'Morpheus-Code'                 : 'hvmUpdater',
            'Morpheus-Description'          : 'Track, download, and apply HPE Morpheus / VM Essentials releases from My HPE Software Center, with a resumable appliance upgrade runner.',
            'Morpheus-Min-Appliance-Version': '8.1.0'
        )
    }
}

shadowJar {
    archiveClassifier.set('all')
    mergeServiceFiles()
}

tasks.assemble.dependsOn tasks.shadowJar
